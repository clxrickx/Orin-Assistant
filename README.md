Prior to using the code, you'll have to import all the packages from the terminal.

To do this, you can use:
pip install (package name)

or you can use
py -m pip install (package name)

This should get rid of any "xxx could not be resolved" errors.
